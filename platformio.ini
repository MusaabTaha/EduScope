[env:disco_f429zi]
platform = ststm32
board = disco_f429zi
framework = stm32cube

build_flags =
    -std=gnu11
    -Wall
    -Wextra
    -Wshadow

; STM32Cube supplies startup files and the linker script only.
; The application does not call HAL or CubeMX-generated peripheral code.
